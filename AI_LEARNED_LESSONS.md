# AI 助手经验教训记录

**目的**: 记录开发过程中的重要教训，避免重复犯错

---

## 📝 编码问题（2026-04-09）

### ❌ 错误：文件编码转换导致文件损坏

**场景**: 
- PDFNotesPanel.tsx 文件中有乱码（中文注释显示为乱码）
- 原因：文件使用 GBK 编码保存，但被当作 UTF-8 读取

**错误操作**:
```python
# ❌ 错误：直接转换编码会破坏文件
content = open(file, 'r', encoding='gbk').read()
open(file, 'w', encoding='utf-8').write(content)
```

**后果**:
- 文件损坏，编译失败
- 需要从 git 恢复

**正确做法**:
1. **先检查编译是否成功**
   ```bash
   npm run build
   ```

2. **如果编译成功，说明功能正常，但注释乱码也需要修复**
   - 注释是给人看的，影响代码可读性
   - 影响后续维护
   - **需要修复**

3. **安全的修复方法**:
   - **方法 1**: 用编辑器（VSCode）手动转换
     - VSCode 会自动检测编码（GBK）
     - 另存为 UTF-8
     - 最安全
   - **方法 2**: 用脚本转换（风险较高）
     - 先备份文件
     - 转换后检查编译是否成功
     - 如果失败，从备份恢复

**教训**:
- ⚠️ **注释乱码也需要修复**
- ⚠️ **编译成功 = 功能正常，但不代表不需要修复注释**
- ⚠️ **用编辑器手动转换最安全**
- ⚠️ **转换前一定要备份**

---

## 📝 重构问题（2026-04-09）

### ✅ 正确：拆分后必须测试

**场景**: 
- 拆分大文件（api.ts、PDFNotesPanel.tsx 等）
- 每次拆分后都运行 `npm run build` 测试

**正确做法**:
1. 拆分前：记录文件大小和功能
2. 拆分后：立即测试编译
3. 如果失败：立即修复，不要继续拆分
4. 测试通过：再进入下一步

**教训**:
- ✅ **拆分后必须测试**
- ✅ **确保功能完全正常**
- ✅ **每一步都要验证**

---

## 📝 组件合并问题（2026-04-09）

### ✅ 正确：发现重复组件并合并

**场景**: 
- 发现两个 QuarkUploadModal 组件（功能相同）
- 一个在 `components/`，一个在 `components/tagLibrary/`

**正确做法**:
1. 分析两个组件的差异
2. 选择保留设计更好的组件
3. 删除重复组件
4. 更新引用
5. 测试验证

**教训**:
- ✅ **定期检查是否有重复组件**
- ✅ **合并前先对比差异**
- ✅ **合并后必须测试**

---

## 📝 独立服务创建问题（2026-04-09）

### ✅ 正确：创建独立 PaddleOCR 服务

**场景**: 
- 多个项目需要 OCR 功能
- 不想重复安装模型（2-4 GB）

**正确做法**:
1. 复制核心代码到独立目录
2. 创建 FastAPI 服务
3. 创建客户端库 `ocr_client.py`
4. 创建详细使用文档
5. 测试验证（线程安全、顺序正确）

**教训**:
- ✅ **独立服务要完整测试**
- ✅ **提供清晰的客户端库**
- ✅ **提供详细的使用文档**

---

## 📝 文件大小规范

| 文件类型 | 最大行数 | 原因 |
|----------|----------|------|
| **组件文件** | 500 行 | 超过难以维护 |
| **API 文件** | 300 行 | 模块化清晰 |
| **Hook 文件** | 300 行 | 业务逻辑复杂可适当放宽 |

**教训**:
- ⚠️ **超过限制立即拆分**
- ⚠️ **不要等到无法维护**

---

## 📝 测试驱动开发

### 重要原则

1. **拆分后必须测试**
2. **合并后必须测试**
3. **重构后必须测试**
4. **每次修改都要验证**

**教训**:
- ✅ **测试是最后一道防线**
- ✅ **不要跳过测试**

---

## 📝 Git 使用规范

1. **修改前先检查 git 状态**
   ```bash
   git status
   ```

2. **破坏性操作前先备份**
   ```bash
   git stash
   ```

3. **出问题后从 git 恢复**
   ```bash
   git checkout HEAD -- file.tsx
   ```

**教训**:
- ✅ **Git 是最好的备份**
- ✅ **出问题立即恢复**

---

## 📝 如何使用这个文件

### 给 AI 助手

在开始工作前，告诉 AI：
```
请先阅读 AI_LEARNED_LESSONS.md 中的经验教训
```

### 给开发者

定期回顾这个文件，避免重复犯错。

---

## 📝 更新记录

| 日期 | 教训 | 重要性 |
|------|------|--------|
| 2026-04-09 | 文件编码转换导致文件损坏 | 🔴 高 |
| 2026-04-09 | 拆分后必须测试 | 🔴 高 |
| 2026-04-09 | 发现重复组件并合并 | 🟡 中 |
| 2026-04-09 | 独立服务要完整测试 | 🔴 高 |

---

**总结**: 这个文件记录了开发过程中的重要教训，帮助 AI 助手和开发者避免重复犯错，越用越聪明。
